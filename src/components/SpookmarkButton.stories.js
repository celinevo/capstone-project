import SpookmarkButton from './SpookmarkButton.js';

export default {
  title: 'components/SpookmarkButton',
  components: SpookmarkButton,
};

const Template = args => <SpookmarkButton {...args} />;

export const Default = Template.bind({});
